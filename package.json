{
  "name": "netlify-proxy-service",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Netlify Functions proxy service",
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "netlify dev",
    "build": "node --eval \"console.log('Netlify Functions project: no build step required.')\"",
    "test": "node --test"
  }
}
